/safeslice
/safeslice.exe
/dist/
/safeslice.yaml
*.sql.gz
slice.sql
*-keys.db
/.claude/
# Wizard state and run artifacts. Nothing here holds a password, but it
# describes your databases and belongs to a machine, not to the repository.
/.safeslice/
/safeslice-output/
/safeslice-demo/

# The Remotion project that renders the demo. Its source lives on its own
# branch; what is left here is node_modules and rendered output, which is
# 17,000 files of dependencies one `git add -A` away from the history.
/video/
